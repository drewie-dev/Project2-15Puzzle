<?php
// EXAMPLE ONLY — copy this file to config.php and fill in your real
// credentials there. config.php is gitignored and will never be
// committed, so your real password stays private.

$DB_HOST = 'localhost';
$DB_NAME = 'your_database_name';
$DB_USER = 'your_username';
$DB_PASS = 'your_password';